import React from "react";
import { firebaseDatabase } from "../../shared/fire";
import CardMake from "./cardMake";

const CardList = (props) => {
  let userId = props.userEmail;

  return <CardMake key={null} />;
};

export default CardList;
